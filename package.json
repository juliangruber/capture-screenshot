{
  "name": "capture-screenshot",
  "version": "2.0.5",
  "license": "MIT",
  "repository": "juliangruber/capture-screenshot",
  "description": "Capture screenshots in multiple browsers headlessly",
  "scripts": {
    "test": "prettier-standard '**/*.js' && standard && node example"
  },
  "devDependencies": {
    "prettier-standard": "^8.0.1",
    "standard": "^11.0.1"
  },
  "dependencies": {
    "capture-chrome": "^2.0.0",
    "capture-electron": "^3.0.0",
    "capture-phantomjs": "^1.3.2"
  }
}
